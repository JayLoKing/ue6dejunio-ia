-r requirements.txt

# --- Calidad de codigo ---
# ruff hace las dos cosas: `ruff check` lintea y `ruff format` formatea, con el
# mismo estilo que black. black estaba pinneado aca y sin instalar en el entorno,
# asi que el formateador declarado no era el que nadie corria.
ruff==0.8.4
mypy==1.13.0

# --- Pruebas ---
pytest==8.3.4
pytest-cov==6.0.0

# --- Exploracion ---
jupyterlab==4.3.3
matplotlib==3.9.3
seaborn==0.13.2
